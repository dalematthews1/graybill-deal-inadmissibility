import GraybillDeal.UniversalCanonicalComponentDensity

/-!
# Axiom audit for the canonical component-density layer

The three printed theorems cover:

* one-dimensional positive scaling of a Gamma density;
* the permutation and componentwise scaling of `(V,U₁,U₂)` into
  `(g₁,g₂,w)`;
* the resulting explicit nested-triple product density.
-/

#print axioms GraybillDeal.gammaMeasure_map_positiveScale
#print axioms GraybillDeal.map_universalCanonicalGammaScaling_riskTiltedMeasure
#print axioms GraybillDeal.map_universalCanonicalGammaScaling_eq_scaledProductDensity
